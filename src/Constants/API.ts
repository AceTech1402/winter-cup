const BASE_URL = "https://api.goldensports.at";

export const API = {
  auth: {
    addRequest: `${BASE_URL}/request/addRequest_eurochallenge`,
    countryNames: `${BASE_URL}/request/country_names`,
  },
};